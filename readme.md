# Dockerized Flask App with CI

This project demonstrates how to Dockerize a simple Python Flask application
using Docker and Docker Compose, with automated CI using GitHub Actions.

## Tech Stack
- Python (Flask)
- Docker
- Docker Compose
- GitHub Actions

## How to Run Locally

### Build and start containers
```bash
docker compose up --build
